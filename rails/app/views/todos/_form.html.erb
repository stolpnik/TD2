<%= form_for([@todo.list, @todo]) do |f| %>
  <% if @todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

      <ul>
      <% @todo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <!--<div class="field">-->
    <%= f.label :done %><br>
    <!--<%= f.text_field :done %>-->
  <!--</div>-->
  <div class="field">
    <%= f.label :start_at %><br>
    <%= f.datetime_select :start_at %>
  </div>
  <div class="field">
    <%= f.label :finish_at %><br>
    <%= f.datetime_select :finish_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
